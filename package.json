{
  "name": "anglo_diver",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client": "cd client && npm install && npm run dev",
    "server": "cd server && npm install && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "lint": "eslint \"client/src/**/*.{js,jsx}\" \"server/**/*.js\" --fix"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.39.2"
  }
}
